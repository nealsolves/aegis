# Codex Prompt: PR-10d Research Safety Addendum Implementation

Use the prompt below as-is with Codex when you want PR-10d implemented from the
local design spec.

```text
Implement AEGIS v0.9.0 PR-10d, the Research Safety Addendum, in the local AEGIS
repository.

Repository root:
/Users/neal/Documents/_Shenanigans/_myProjects/aegis

Target branch:
feat/v0.9-10d-research-safety-addendum

Primary design source:
docs/design/v0.9.0_PR-10d_RESEARCH_SAFETY_ADDENDUM_DESIGN_SPEC.md

Supporting plan:
docs/plans/v0.9.0_PR-10d_RESEARCH_SAFETY_ADDENDUM_PLAN.md

Task type:
- Implement code, tests, and docs for PR-10d
- Keep the work bounded to the design spec
- Do not turn AEGIS into a runtime, orchestrator, proxy, MCP gateway, memory manager, streaming runtime, formal checker, or LLM-as-judge engine
- Do not add a new required runtime dependency
- Do not expose internal ValidatorHook examples as public API or public docs

Initial working rules:
- Capture `git status -sb` before editing
- Confirm the current branch and branch base before making changes
- The worktree may be dirty; do not revert or disturb unrelated local changes
- Read the full design spec before implementation, especially Sections 6 through 17
- Treat the design spec as authoritative over the shorter prompt pack in the plan
- Use existing repo patterns and tests before inventing new abstractions
- Keep public CLI human output stable unless the design spec explicitly allows a change
- Do not add remediation text, `severity`, or `next_action` to lint findings
- Keep doctor as the owner of severity and next-action guidance
- Preserve raw-provider-payload redaction boundaries in export paths

PR-10d scope:
1. Add deterministic workflow graph/topology lint over existing policy DSL fields.
2. Add bounded `details` and `witness_trace` evidence to new static lint findings.
3. Keep existing lint finding keys and CLI human output compatible.
4. Add doctor mappings and doctor-only provenance warnings.
5. Add backward-compatible workflow export governance rationale projection.
6. Add safety smoke tests and internal ValidatorHook example tests.
7. Add adapter fixture hardening tests only for adapter surfaces present in the target branch.
8. Update reference docs and release-truth docs.

Files to inspect first:
- aegis/_internal/workflow_lint.py
- aegis/_internal/workflow_doctor.py
- aegis/_internal/workflow_export.py
- aegis/_internal/session.py
- aegis/_internal/starter_templates.py
- aegis/_internal/validator_hook.py
- aegis/schemas/policy_dsl.schema.json
- aegis/schemas/workflow_artifact.schema.json
- tests/test_workflow_lint.py
- tests/test_workflow_doctor.py
- tests/test_workflow_export.py
- tests/test_validator_hook.py
- tests/test_openai_agents_adapter.py
- tests/test_openai_agents_adapter_integration.py
- tests/test_v090_contract_freeze.py
- tests/test_public_api.py

Files likely to edit:
- aegis/_internal/workflow_lint.py
- aegis/_internal/workflow_doctor.py
- aegis/_internal/workflow_export.py
- aegis/_internal/starter_templates.py, only if needed for deterministic starter smoke tests
- tests/test_workflow_lint.py
- tests/test_workflow_doctor.py
- tests/test_workflow_export.py
- tests/test_pr10d_research_safety_addendum.py
- tests/test_validator_hook_internal_examples.py
- adapter fixture tests only if matching adapter surfaces exist
- docs/reference/TROUBLESHOOTING.md
- docs/reference/WORKFLOW_CLI.md
- docs/reference/STARTER_RECIPES.md
- docs/reference/OPERATIONS_RUNBOOK.md
- RELEASE_GATES.md
- implementation_status.md
- docs/dev/pr_context.md

Do not edit public docs to show `_internal` imports as valid user-facing usage.

Implementation order:

Phase 1: Establish context
- Capture `git status -sb`
- Capture `git branch --show-current`
- Read the PR-10d design spec and plan
- Confirm which adapter surfaces exist in the target branch:
  - Bedrock PR-10a
  - A2A PR-10b
  - OpenAI Agents PR-10c
- If Bedrock or A2A surfaces are absent, document deferred adapter-specific gates instead of adding failing tests for missing code.

Phase 2: Lint finding shape and graph helpers
- Extend `aegis/_internal/workflow_lint.py` `_finding(...)` additively:
  - required existing keys: `code`, `message`, `target_kind`, `path`
  - optional `details`
  - optional `witness_trace`
- Add `_MAX_WITNESS_TRACE_EVENTS = 8`.
- Ensure optional fields are JSON-serializable and bounded.
- Keep human CLI output unchanged. JSON output may include optional fields naturally.
- Add graph helpers named in the design spec:
  - `_lint_workflow_graph(...)`
  - `_normalize_required_sequence(...)`
  - `_normalize_transition_graph(...)`
  - `_find_reachable_steps(...)`
  - `_first_required_sequence_transition_conflict(...)`
  - `_find_unbounded_handoff_cycle(...)`
- Call graph lint from `lint_policy(...)` only after schema validation and existing reference checks.
- If existing `WORKFLOW_INVALID_TRANSITION` unknown-step or unknown-participant findings exist, skip transition topology checks that depend on those references.

Phase 3: Add graph/topology lint checks
- Implement new additive lint codes:
  - `WORKFLOW_UNREACHABLE_STEP`
  - `WORKFLOW_DEAD_END_STEP`
  - `WORKFLOW_REQUIRED_SEQUENCE_IMPOSSIBLE`
  - `WORKFLOW_UNBOUNDED_HANDOFF_LOOP`
- Keep existing codes where they already apply:
  - `WORKFLOW_STEP_BUDGET_EXCEEDED` remains the code for `len(required_sequence) > max_steps`
  - `WORKFLOW_INVALID_TRANSITION` remains the code for undeclared step or participant references
- Enrich `WORKFLOW_STEP_BUDGET_EXCEEDED` with `details`:
  - `required_sequence_length`
  - `max_steps`
  - `conflicting_rule`
- Do not add `WORKFLOW_REQUIRED_SEQUENCE_IMPOSSIBLE` for the same budget conflict unless there is a separate transition conflict.
- Use `workflow.required_sequence` as the declared step universe.
- Skip step graph checks when `required_sequence` is missing or empty.
- Skip transition graph checks when `allowed_transitions` is missing or empty.
- Do not infer sensitive, external, terminal, source, or approval semantics from arbitrary step names.
- Emit at most one finding per new code per policy file in the first cut.
- Use deterministic first-conflict selection based on `required_sequence` order and sorted participant IDs.

Required graph lint behavior:
- `WORKFLOW_UNREACHABLE_STEP`: a declared step cannot be reached from `required_sequence[0]` through `allowed_transitions`.
- `WORKFLOW_DEAD_END_STEP`: a non-terminal required step has no valid declared successor in `allowed_transitions`.
- `WORKFLOW_REQUIRED_SEQUENCE_IMPOSSIBLE`: a consecutive required pair is not allowed by `allowed_transitions`.
- `WORKFLOW_UNBOUNDED_HANDOFF_LOOP`: `workflow.handoffs` contains a participant cycle with no `max_steps`, no `escalation.require_approval_after_steps`, and no `escalation.require_approval_for_roles` intersection with roles in the detected cycle.

Phase 4: Doctor mappings and provenance warnings
- Add `_NEXT_ACTIONS` entries in `aegis/_internal/workflow_doctor.py` for every new lint code and doctor-only code.
- Keep `_lint_to_doctor(...)` promoting lint findings to `ERROR`.
- Add doctor-only `WORKFLOW_SOURCE_PROVENANCE_WARNING` with severity `WARNING`.
- Implement exact-key memory/source checks only. Use:
  - `memory`
  - `memories`
  - `conversation_memory`
  - `retrieved_memory`
  - `retrieved_context`
  - `knowledge_base_context`
- Workflow artifact provenance warning:
  - inspect `steps[i].metadata.governance`
  - if exact governance metadata declares source-bearing behavior but lacks `source_ids`, warn
  - exact markers include `decision_basis` containing `provenance.source_ids` or `source_required: true`
  - if workflow metadata includes memory-like keys without source IDs, warn
- Audit artifact provenance warning:
  - if `context` contains memory-like fields and neither `context.provenance.source_ids` nor top-level `provenance.source_ids` exists, warn
- This warning must not block lint, export, default starter execution, or doctor exit behavior unless another `ERROR` exists.

Phase 5: Workflow export governance rationale
- In `aegis/_internal/workflow_export.py`, add helpers:
  - `_extract_governance_summary(step: dict[str, Any]) -> dict[str, Any] | None`
  - `_redact_governance(governance: dict[str, Any]) -> dict[str, Any]`
- Source artifact shape is `workflow_artifact.steps[i].metadata.governance`.
- Redacted governance summary may keep only canonical keys:
  - `rationale`
  - `decision_basis`
  - `operator_action`
  - `approval_checkpoint_id`
  - `source_ids`
  - `waiver_id`
- Keep only scalar values, null, and string arrays for canonical keys.
- Drop nested objects and unsupported payloads from governance summaries.
- Operator mode:
  - preserve existing full step metadata pass-through
  - add `integrity.governance_rationale_count`
  - do not remove or reshape embedded invocation artifacts
- Audit mode:
  - continue copying `metadata` when present
  - add a convenience `governance` projection when valid governance metadata is present
  - omit malformed governance projections rather than failing export
- Do not introduce raw provider prompts, tool inputs, trace payloads, or model outputs through PR-10d export changes.

Phase 6: Safety smoke tests
- Create `tests/test_pr10d_research_safety_addendum.py`.
- Cover fixture-only scenarios from the design spec:
  1. Prompt-injection-like source instruction cannot override workflow policy.
  2. External send or handoff blocks without required approval.
  3. Multi-turn failure-and-fix path cannot bypass approval after resume.
  4. Handoff loop is bounded by `max_steps` or explicit approval.
  5. Tool budget cannot be exceeded through step composition.
  6. Adapter capability mismatch fails closed for adapter surfaces present in the target branch.
  7. Export shows governance rationale for blocked, paused, failed, and completed paths.
- Use exact fixture structure, not model interpretation or free-text semantics.
- Tests must run without network, provider accounts, or external model APIs.

Phase 7: Internal ValidatorHook example
- Create `tests/test_validator_hook_internal_examples.py`.
- Implement an internal deterministic multi-aspect hook example using existing `aegis._internal.validator_hook` contracts.
- Cover aspects:
  - source grounding
  - schema semantics
  - policy risk
  - externalization risk
- Cover decisions:
  - `ALLOW` when all aspects pass
  - `DENY` for missing source grounding in source-required fixture context
  - `REVIEW_REQUIRED` for explicit externalization-risk fixture labels
  - timeout fail-closed behavior through existing hook invocation helpers
  - stale result behavior if the current hook contract exposes it
  - artifact hook evidence remains internal
- No public docs, README, generated starter output, or reference integration docs may import from `aegis._internal`.

Phase 8: Adapter fixture gates
- Add adapter-specific fixture tests only for adapter surfaces actually present in this branch.
- For OpenAI Agents surfaces, prefer fakes and local fixtures. Do not import the real provider runtime unless the existing test pattern already makes it optional and skippable.
- Test themes for present adapters:
  - authoritative participant identity, not descriptive name matching
  - capability mismatch fails closed
  - protocol mismatch fails closed
  - side-effecting or externalizing capability requires approval when policy demands approval
  - remote participant cannot widen role, protocol, capability, or handoff authority
  - unsupported dynamic tool surfaces are explicitly rejected
- If Bedrock or A2A source files are absent, add deferred gate notes to:
  - RELEASE_GATES.md
  - implementation_status.md
  - docs/dev/pr_context.md

Phase 9: Documentation
- Update:
  - docs/reference/TROUBLESHOOTING.md
  - docs/reference/WORKFLOW_CLI.md
  - docs/reference/STARTER_RECIPES.md
  - docs/reference/OPERATIONS_RUNBOOK.md
  - RELEASE_GATES.md
  - implementation_status.md
  - docs/dev/pr_context.md
- Document:
  - new lint codes and meanings
  - `workflow lint --json` includes optional bounded `details` and `witness_trace`
  - lint does not own `severity` or `next_action`
  - doctor remains the remediation owner
  - `WORKFLOW_SOURCE_PROVENANCE_WARNING` is non-blocking by default
  - export governance rationale metadata and redaction boundaries
  - deferred adapter-specific gates when Bedrock or A2A are absent
  - internal ValidatorHook examples remain internal tests only

Phase 10: Verification
- Run focused tests:
  `python -m pytest tests/test_workflow_lint.py tests/test_workflow_doctor.py tests/test_workflow_export.py tests/test_pr10d_research_safety_addendum.py tests/test_validator_hook_internal_examples.py`
- Run public contract tests:
  `python -m pytest tests/test_v090_contract_freeze.py tests/test_public_api.py`
- If adapter tests changed, run the relevant adapter test files.
- If docs changed, run the repo's doc parity or truth checks if available.
- If the package smoke command exists in the repo, run the strongest local package smoke that does not require network credentials.
- Do not claim tests passed unless you actually ran them. If a command cannot be run, record the exact blocker.

Acceptance criteria:
- Graph/topology lint checks are deterministic and test-backed.
- Every new lint finding has stable `code`, plain-English `message`, and bounded evidence where applicable.
- No lint finding includes `severity` or `next_action`.
- Doctor maps every new lint and doctor-only code to severity and exact remediation guidance.
- Source provenance warnings are doctor-owned and non-blocking by default.
- Workflow export includes backward-compatible governance rationale projection.
- Export integrity metadata remains present.
- Raw provider payloads are not persisted or projected by default.
- Safety smoke tests pass without external dependencies.
- Adapter fixture tests are present only for adapter surfaces available in the target branch.
- Missing adapter-specific gates are recorded as deferred, not hidden.
- Internal ValidatorHook examples do not leak into public docs or starter output.
- No new required runtime dependency is introduced.
- Public API freeze tests still pass.
- Package smoke still passes for base install.

Final response requirements:
- Start with a concise implementation summary.
- List changed files by area.
- State the exact tests and checks run.
- Note any skipped/deferred adapter gates with the reason.
- Note any residual risk or open question from the design spec that remains unresolved.
```
